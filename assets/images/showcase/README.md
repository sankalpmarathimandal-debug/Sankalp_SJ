# Showcase images

Drop photos here (community art, performances, achievements — anything you want on the
Showcase page as an image rather than a video) and reference the filename in the
`ImageURL` column of `data/showcase.xlsx`, e.g. `assets/images/showcase/priya-painting.jpg`.

Keep images web-friendly: resize to roughly 1200px on the long edge, JPG preferred.
